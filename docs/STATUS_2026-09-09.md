# Agent-Rider live lock · 2026-09-09

Private team status. No secrets. No api_keys.

## Hosts

| Surface | Status |
|---|---|
| https://agentrider.fly.dev | Live. health ok. rider + db + stripe ready. MCP initialize works. JWKS live. |
| https://agentrider.fly.dev/api/mcp | Live Streamable HTTP. 63 tools advertised. |
| https://agentrider.vercel.app | 402 DEPLOYMENT_DISABLED. Do not point Glama here. |
| CuNi Studio remote | Points at Fly. Remote contracts = 0. |

Probed 2026-09-09 ~02:20 AM EDT: `participants: 0`, `openTasks: 0`.

## Blocker

Register returns 201. Issue returns 401 `invalid_api_key`.

`src/lib/agents.ts` `registerParticipant` swallows a failed Supabase INSERT and writes `data/participants.json` on the Fly box. `resolveByApiKey` only reads Supabase. Documented in `supabase/schema.sql` (2026-07-14 merge): `service_role` lost INSERT on `participants`.

Ghost ids from this session (disk only — not ledger identities):

- Grok `81ff28d379b7fa32`
- Benjamin `750bdef0ed0f1204`
- Idea thinktank `d38fd74479a095f9`
- business Guru `60db119a4ae5f55b`
- LiveCheck `bcf7f529ca394b8c`

Do not mint more keys until INSERT works.

## GRANT to run

```sql
GRANT ALL ON ALL TABLES IN SCHEMA public TO service_role;
GRANT ALL ON ALL SEQUENCES IN SCHEMA public TO service_role;
GRANT ALL ON ALL FUNCTIONS IN SCHEMA public TO service_role;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO service_role;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO service_role;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON FUNCTIONS TO service_role;
```

After GRANT: re-register with `operator_id=slidphilabs`, issue L1, confirm health.participants > 0. Then make failed INSERT return 500 instead of fake-201.

## Pointers

- Notion: https://app.notion.com/p/3d685ac68da181118b8fe225036ace94
- Drive folder (empty, no create-file tool from this session): https://drive.google.com/drive/folders/1lKfqNJnL-rgeiubAikJJeY-dmaQ805Zz
