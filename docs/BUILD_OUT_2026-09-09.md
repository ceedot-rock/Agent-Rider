# Build-out · 2026-09-09 (superseded by product/first-job-loop)

Branch `product/first-job-loop` completes the self-serve product loop promised by issue/PR #10.

## Surfaces

- `POST /api/start` — register + attempt L1 issue (rate-limited); honest `store` / `db_error`
- `src/lib/identity.ts` — `resolveCaller` from rider or api_key
- `src/lib/client-session.ts` — browser session for /start, /first-job, /desk
- `/start` — name → identity card
- `/first-job` + `POST /api/first-job` — practice poster escrows 5 AGC; claim/submit; immediate approve
- `/desk` + `GET|POST /api/desk` — post real work; approve/reject

Same `postTask` / `claimTask` / `submitTask` / `approveTask` as the public market. No Autonoma/Blackjack IP.

## Still blocked on live Fly

- GRANT for `service_role` INSERT on `participants`
- Marketplace empty until GRANT + deploy
- Vercel 402. Official host remains https://agentrider.fly.dev
