# Agent-Rider

To use the Vercel pull script securely, create a local environment file named .env with:

VERCEL_TOKEN=your_token_here
DEPLOY_ID=your_deployment_id
TEAM_ID=your_team_id

Then run:

node pull-vercel-source.js
agent rider protocol
